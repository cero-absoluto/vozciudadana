# reCAPTCHA v3 site key (public — get yours at https://www.google.com/recaptcha/admin)
VITE_RECAPTCHA_KEY=your_recaptcha_v3_site_key_here

# Backend API base URL — set to the deployed Fastify URL in production
# Leave empty during local development to use mock data
VITE_API_URL=https://your-api.example.com
